Todo:

1. Use inject config or similar on data.js, docDef.js, webworker if required.
2. Replace hard coded values in data.js with fetched config values
3. Replace hard coded values in sanitiseAndValidate with config.json values which match those used by data.js

4. Agree update to static version ICP to 1.4 - replace the fallback version hosted on site

Before launch:

1. Test limits
2. Legacy?
3. htaccess redirect for app level routes
4. remove from assets/api.js:
   //temp addition during API testing...
   console.log(JSON.stringify(data));
5. add date/time to changelog and contributors
